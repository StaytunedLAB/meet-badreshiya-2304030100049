// Type or paste this directly into the browser console

console.log("Hello from the Developer Console!");

let name = "Developer";
let count = 3;

for (let i = 1; i <= count; i++) {
  console.log(i + ". Welcome, " + name + " 👨‍💻");
}

// Try interacting with the page
document.body.style.backgroundColor = "lightyellow";
console.log("Changed the page background color using JavaScript!");
